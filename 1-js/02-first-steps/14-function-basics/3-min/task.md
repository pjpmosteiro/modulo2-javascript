importance: 1

---

# Function min(a, b)

Escriba una función `min(a, b)` que devuelva el menor de los dos números `a` y`b`.

Por ejemplo:

```js
min(2, 5) == 2;
min(3, -1) == -1;
min(1, 1) == 1;
```
